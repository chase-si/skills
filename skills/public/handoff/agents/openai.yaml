interface:
  display_name: "Handoff"
  short_description: "Compact context for another agent to continue"
  default_prompt: "Use $handoff to create a compact handoff for another agent."
policy:
  allow_implicit_invocation: false
