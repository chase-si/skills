interface:
  display_name: "To PRD"
  short_description: "Synthesize conversation context into a PRD"
  default_prompt: "Use $to-prd to synthesize the current context into a PRD."
policy:
  allow_implicit_invocation: false
